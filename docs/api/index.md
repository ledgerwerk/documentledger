# API reference

The stable user interface is the canonical CLI and persisted schemas. Python integration is supported only for the documented, typed modules below; command registration modules and other implementation details are internal unless explicitly labeled. Autodoc does not expose every undocumented symbol as public API.

```{toctree}
:maxdepth: 2

project-and-config
storage-and-scanning
indexing-and-links
rendering-and-impact
migration-and-errors
cli-internals
```

<!-- docledger-section: api-support-policy -->

## Support policy

Supported Python interfaces have useful docstrings, typed parameters and returns, documented exceptions, stable semantics, and tests. Internal pages explain implementation boundaries without promising compatibility.
