# Changelog

> **Generated by releaseledger.** Manual edits may be overwritten. Update releaseledger entries and rebuild this file instead.

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [v0.2.1] - 2026-08-20

### Added

- Documentation generation script for automated CLI reference
- Freshness tracking module for document staleness detection

### Changed

- Simplified CLI entry point with delegated command routing
- Improved render module with additional formatting and impact analysis
- Added test suites for bootstrap workflow, documentation contract, links batch, render, and status
- Enhanced link resolution with expanded batch processing and import-map support
- CLI reference, API docs, configuration guides, quickstart, and troubleshooting documentation
- Added Click as an explicit runtime dependency for reliable CLI installation
- Improved durable document record reconciliation with live sections and linked orphan repair

<!-- generated by releaseledger -->

## [v0.2.0] - 2026-07-28

### Added

- Command catalog and CLI support modules for centralized command registration and version handling ({ref}`Architecture: CLI structure and errors <cli-structure-and-errors>`)
- Structured error handling module with typed exceptions for better diagnostics ({ref}`Architecture: CLI structure and errors <cli-structure-and-errors>`)
- Launcher module for unified entry point configuration ({ref}`Architecture: CLI structure and errors <cli-structure-and-errors>`)
- Modular CLI architecture with dedicated command modules for documents, sources, links, storage, migration, configuration, and schema management ({ref}`Architecture: CLI structure and errors <cli-structure-and-errors>`)

### Changed

- CLI entry point refactored to use modular command groups with canonical singular naming (document, source, link) ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- Upgraded ledgercore dependency from 0.5.0 to 0.6.0 with schema-3 canonical initialization ({ref}`Architecture: Storage model <storage-model>`)
- Migrated releaseledger and taskledger storage to unified .ledger/ directory structure ({ref}`Architecture: Storage model <storage-model>`)
- Updated README and documentation with new CLI command structure and migration guides ({ref}`Bootstrap: Setup sequence <setup-sequence>`)

<!-- generated by releaseledger -->

## [v0.1.1] - 2026-07-11

### Added

- New `sources list` CLI with filtering, pagination, and compact ids-only output ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- New `docs sections --outline` compact section listing ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- New `links import-map` command with directory batch mode, validate-only dry run, and replace-section support ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- New `links propose --all-docs --out-dir` for auto-generated mapping proposals ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- New `coverage` command for reviewing link coverage inventory ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- Build context selectors for bootstrap, doc/section, affected, and max-bytes truncation ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)

### Changed

- Storage layer rewritten with atomic staged writes, source index separation, and ledgercore integration ({ref}`Architecture: Storage model <storage-model>`)
- Read-only CLI commands no longer mutate storage metadata ({ref}`Architecture: Storage model <storage-model>`)
- Scan schema bumped to v5 with separate source-index.json and SHA-256 hash verification ({ref}`Architecture: Scanning algorithm <scanning-algorithm>`)
- Doc index parser now ignores headings inside fenced code blocks ({ref}`Architecture: Scanning algorithm <scanning-algorithm>`)
- CLI pagination with cursor-based offset, and structured error for invalid cursors ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- Reworked SKILL.md with entry protocol, bootstrap branch, incremental branch, and completion gate ({ref}`Bootstrap: Setup sequence <setup-sequence>`)
- Added CommandState caching for scan, source index, and doc records to reduce redundant I/O ({ref}`Architecture: Scanning algorithm <scanning-algorithm>`)
- Added test suites for build context selectors, CLI pagination, doc index, links batch, scan incremental, storage read-only, and workflow extras ({ref}`Usage: Link documentation to sources <link-documentation-to-sources>`)
- Updated conftest with JSON helpers and storage snapshot hashing ({ref}`Architecture: Scanning algorithm <scanning-algorithm>`)

<!-- generated by releaseledger -->

<!-- releaseledger:generated-file -->
